let guestList = ["Alex" , "Dravid" , "Gayle"];
 
//guestList.forEach(guest => console.log(`Dear ${guest}, I would be honored to have you join me for dinner.`));

let guestNumber: number = guestList.length;

console.log(`We are inviting total ${guestNumber} guests.`);